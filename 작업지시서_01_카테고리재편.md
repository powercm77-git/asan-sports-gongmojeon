# 작업지시서 01 — 업종별 지도를 아산시 유관기관 지도로 재편

## 먼저 할 일
이 저장소의 `CLAUDE.md`를 먼저 읽는다. 이 프로젝트가 왜 시작됐는지, 원본 `biz-eng-map`과의
관계가 무엇인지 그 문서에 있다. 그다음 같은 폴더의 `기본정보_조사자료.md`를 읽는다 — 카테고리
콘텐츠를 채울 때 쓸 실제 조사 자료다. **2026-09-19에 이 조사자료가 한 번 더 개정됐다** —
일반 웹검색 대신 카카오맵(map.kakao.com)에서 기관명을 직접 검색해 대조했더니 주소 오류
2건(보건소, 아산가족센터)이 나왔고, 체육시설·행정복지센터·교통시설 3개 카테고리의 커버리지
방식이 아래처럼 다시 설계됐다. 이 문서(작업지시서)도 그에 맞춰 개정했다 — 이전에 이 지시서를
읽은 적이 있어도 2번·3번 항목은 다시 읽는다.

## 지금 index.html 상태 (중요 — 반드시 먼저 확인)
이 지시서는 이미 한 번 실행됐다. 아래는 지금 `index.html`에 이미 반영된 것과, 아직 고쳐야
할 것이다.

**이미 잘 된 것 (건드리지 않는다):**
- 1) 타이틀 교체 — 완료.
- 2) 12개 유관기관 카테고리 교체, Kakao 코드/keywordSearch 구분 — 완료.
- 3) 2단계(나머지 11개 기관 플레이스홀더 4개씩 + 주석) — 완료.
- 12개 언어 선택, 번역·음성인식·TTS 엔진 — 그대로 살아있음.
- 지시서 범위 밖이지만 코드 세션이 같이 고친 것: 지도 기본 중심좌표(평택시→아산시청), 기본
  선택 카테고리(CS2→체육시설) — 잘한 판단이니 그대로 둔다.

**고쳐야 할 것:**
- 3) 1단계(체육시설 SCENES.SPT, 지금 15개 시나리오)가 문제다. 수영장·헬스장 이용 문의처럼
  **실제 복합스포츠센터에 없는 시설**로 채워져 있다 — `sports_app_bilingual.html` 원천 자료를
  찾기 전에 지어낸 내용이다. 지금 있는 SPT 15개 시나리오는 전부 지우고, 아래 1단계 지시대로
  실제 자료로 다시 채운다.
- 4) 교육어플 연결 메뉴 — 아직 반영 안 됨. 아래 4번 지시대로 새로 추가한다.
- 5) 관광시설 카테고리 — 아직 없음. 아래 2번 지시에 새로 추가된 내용이다.
- 6) 병원·약국·교통시설 3개 카테고리 — 아직 없음. 원본 biz-eng-map에 있던 카테고리인데
  아산시 유관기관 중심으로 바꾸면서 빠졌다. 다문화 주민·관광객에게 필요한 공공서비스 성격이
  강해서 다시 넣는다. 아래 2번 지시에 새로 추가된 내용이다.
- 7) 2단계 플레이스홀더 내용의 성격이 바뀌었다 — "아무 정보나 되는 일반 표현"이 아니라
  `기본정보_조사자료.md`에 정리해 둔 실제 조사 결과([확인됨] 항목)를 반영해야 한다. 아래
  3번 지시 참고.
- 8) **(2026-09-19 신규)** 체육시설(SPT) 카테고리가 복합스포츠센터 하나만 검색하도록 돼
  있는데, 이건 아산시 전체를 대표하지 못한다. 아래 2번 지시대로 5개 공공체육시설을 함께
  검색하도록 바꾼다.
- 9) **(2026-09-19 신규)** 행정복지센터(GOV)가 "대표 정보만" 수준으로 남아 있다면, 아래
  2번 지시대로 17개 읍면동 전체를 검색하는 루프로 바꾼다.
- 10) **(2026-09-19 신규)** 교통시설이 `SW8`(지하철역) 코드로 돼 있다면, 아래 2번 지시대로
  4개 시설명 keywordSearch 방식으로 바꾼다 — `SW8`은 KTX역·버스터미널을 잡지 못할 가능성이
  크다.
- 11) **(2026-09-19 신규)** 보건소·아산가족센터·외암민속마을의 주소가 `기본정보_조사자료.md`
  "이번에 정정된 것"에 나온 옛 주소로 들어가 있다면 새 주소로 고친다.

## 목적
`index.html`은 지금 업종별(편의점·카페·음식점 등 16개) 지도로 돼 있다. 이걸 **아산시 유관기관
지도**로 바꾼다. 번역·음성인식·TTS 엔진은 이미 완성돼 있고 잘 동작하니 **건드리지 않는다** —
바꾸는 건 타이틀, 카테고리 목록, 그리고 카테고리별 대화 콘텐츠뿐이다.

## 변경 범위 (이 네 가지만)

### 1) 타이틀
상단 좌우 타이틀("실전영어회화!-AI 취업역량강화" / "IT융합컴퓨터과 송정호")을 다음으로 바꾼다.
- 왼쪽: `다(多)함께 통(通)하는 아산`
- 오른쪽: `유관기관 다국어 이용안내`

### 2) 카테고리 목록 교체 (2026-09-19 재설계)

기존 16개 업종 칩을 아래 16개 카테고리로 교체한다. **카테고리 개수는 그대로 16개지만,
체육시설·행정복지센터·교통시설 3개의 내부 구현 방식이 이번에 바뀌었다** — 카테고리 하나가
기관 하나만 검색하는 게 아니라, 여러 기관명을 함께 검색해서 한 카테고리 안에 여러 마커가
뜨도록 한다("멀티 키워드" 방식). 지도 검색 방식(카테고리 코드 vs keywordSearch)은 기관마다
다음 표대로 확정한다 — 어느 것도 실제로 API를 붙여서 재확인은 해야 하지만, 어떤 방식을 쓸지는
`기본정보_조사자료.md`에서 카카오맵으로 이미 확인했다.

1. **체육시설** — `KEYWORD_QUERY`를 배열로 확장해 아래 10개 명칭을 모두 검색하고 결과를
   합친다: "아산시복합스포츠센터", "이순신종합운동장", "아산건강문화센터", "아산배미수영장",
   "아산시 배방북수스포츠센터", "배방공수스포츠센터", "아산시민체육관", "둔포국민체육센터",
   "아산장애인국민체육센터", "한마음야구장 아산국민체육센터". (기존처럼 복합스포츠센터
   하나만 검색하지 않는다 — 배미동·배방읍에도 시설이 더 있다는 걸 사용자가 확인해줘서
   추가했다. 자세한 주소는 조사자료 B그룹 참고. 이후에도 실무자 확인으로 더 늘어날 수
   있는 목록이다.)
2. 보건소 — keywordSearch("아산시보건소"). 주소 정정 반영(번영로224번길 20).
3. **행정복지센터** — 고정된 기관명 하나가 아니라, 아산시 행정구역 17개(읍 2·면 9·행정동 6,
   목록은 조사자료 C그룹 참고) 이름 각각에 "행정복지센터"를 붙여 17번 keywordSearch를 돌리고
   결과를 합친다.
4. 아산가족센터 — keywordSearch("아산시가족센터"). 주소 정정 반영(아산로 13).
5. 문화시설 (기존 `CT1` 코드 재사용)
6. 도서관 (기존 `LIB` 카테고리 그대로 유지)
7. 아산과학관·아산생태곤충원
8. 청소년문화의집·청소년교육문화센터 — 카카오 확인 결과 사실상 같은 기관(배방읍 연화로 29)
9. 여성새로일하기센터·통합일자리지원센터 — 같은 건물(배방읍 배방로 5 충무빌딩 7층)
10. 아산시육아종합지원센터
11. 농업기술센터
12. 평생학습문화센터
13. 관광시설 — Kakao 관광명소 표준 코드 `AT4`를 우선 시도한다(다른 기관처럼 keywordSearch로
    갈 필요 없음). 실제로 안 잡히면 그때 keywordSearch로 바꾼다.
14. 병원 — 원본 biz-eng-map의 `HP8` 카테고리 코드 그대로 재사용.
15. 약국 — 원본 biz-eng-map의 `PM9` 카테고리 코드 그대로 재사용.
16. **교통시설** — `SW8`(지하철역) 코드 시도 단계를 생략하고, 체육시설과 같은 멀티 키워드
    방식으로 바로 간다: "천안아산역", "아산역", "아산고속버스터미널", "아산시외버스터미널"
    4개를 검색해 합친다. `SW8`은 지하철역 전용이라 KTX역·버스터미널이 애초에 안 잡힐
    가능성이 높다.

다문화 주민·관광객에게 필요한 공공서비스라는 이유로 14~16번을 다시 넣기로 했다(원래
biz-eng-map 16개 업종 중 아산시 유관기관 재편 때 함께 빠졌던 것). 숙박(AD5)·은행(BK9)처럼
민간 상업시설 성격이 강한 나머지 업종은 이번에는 넣지 않는다 — 필요하면 나중에 검토한다.

**멀티 키워드 구현 메모**: 지금 코드의 `KEYWORD_QUERY`는 카테고리 하나에 검색어 하나가
매핑된 구조로 보인다. 체육시설·행정복지센터·교통시설 3개는 검색어 배열을 받아 여러 번
`keywordSearch`를 호출하고 결과 마커를 한 레이어에 합치는 함수가 필요하다 — 기존
`KEYWORD_QUERY` 구조를 깨지 않으면서 이 3개만 배열을 받도록 확장하는 걸 권장한다(예:
`KEYWORD_QUERY.GOV`를 문자열 대신 문자열 배열로 바꾸고, 검색 함수에서 배열이면 반복
호출하도록 분기).

### 3) 카테고리별 대화 콘텐츠 — 16개 전부 실제 콘텐츠로 채운다

**중요한 정정**: 이전 버전 지시서는 체육시설만 실제 콘텐츠로 채우고 나머지는 대충 4개짜리
플레이스홀더로 남기라고 했었다. 틀렸다 — **16개 카테고리 전부 실제 콘텐츠가 있어야 한다.**
"플레이스홀더"라고 대충 채워도 되는 건 없다. 기관마다 확보한 정보의 양이 달라서 아래처럼
세 그룹으로 나눠 작업한다.

**그룹 A — 원천 자료를 그대로 옮기는 카테고리 (지어낼 필요 없음):**
- **체육시설**: 실제 대화 시나리오는 복합스포츠센터 몫만 채운다 —
  `아산시유관기관교육어플/sports_app_bilingual.html`의 `LANG_DATA.en.dial`(17개 상황 실제
  한국어-영어 대화)을 그대로 옮긴다. 지금 있는 SCENES.SPT 15개(수영장·헬스장 등 실제로 없는
  시설)는 전부 지우고 이걸로 교체한다. 같은 카테고리에 함께 뜨는 이순신종합운동장·
  둔포국민체육센터·장애인국민체육센터·한마음야구장은 시나리오 없이 카카오 기본 정보(주소·
  전화·운영시간)만 뜬다 — 이걸로 충분하다(2번 지시 "멀티 키워드" 참고).
- **문화시설(CT1)·도서관(LIB)**: 원본 biz-eng-map의 기존 시나리오를 그대로 둔다. 이미 잘
  됐으니 손대지 않는다.
- **병원(HP8)·약국(PM9)**: 원본 biz-eng-map에 이미 있는 시나리오(진료 예약·접수·증상 설명,
  처방전 등 범용 표현)를 그대로 가져온다. 특정 병원 이름이나 가격이 아니라 보편적인 방문
  대화라 그대로 재사용해도 지어낸 게 아니다.
- **교통시설**: 원본 biz-eng-map의 지하철(SW8) 시나리오(교통카드 충전, 노선 안내, 환승,
  분실물 문의)를 가져오되, "지하철" 표현을 아산 실정(기차역·버스터미널)에 맞게 다듬는다.
  대상 4개 시설(천안아산역·아산역·아산고속버스터미널·아산시외버스터미널) 모두에 공통으로
  쓸 수 있는 표현으로 만든다 — 시설별로 다른 대화를 새로 만들 필요 없다.

**그룹 B — 실제 조사 자료로 새로 채우는 카테고리 (2026-09-19 확대):**
`기본정보_조사자료.md`에 실제 운영시간·위치·입장료·업무 내용을 조사해 정리해 놓았다.
[확인됨]이라고 표시된 항목만 그대로 쓰고, 그 정보를 기반으로 운영시간 문의·위치 안내·입장료
문의 같은 대화 시나리오를 새로 만든다. **이번 개정으로 카카오맵 직접 확인 덕분에 3개 기관이
그룹 C에서 그룹 B로 올라왔다** — 대상: 보건소, 아산가족센터, 아산과학관(장영실과학관),
아산생태곤충원, 청소년문화의집·교육문화센터, 농업기술센터, 관광시설(현충사·외암민속마을
기준), **여성새로일하기센터·통합일자리지원센터**(같은 건물 확인됨), **아산시육아종합지원센터**
(위치 확인됨), **평생학습문화센터**(본관 위치 확인됨).

**그룹 C — 정보가 부족해 일반적인 표현만 넣는 카테고리 (2026-09-19 축소):**
`기본정보_조사자료.md`에 [추가확인필요]로 남아 있는 것은 이제 **행정복지센터** 하나뿐이다.
17개 읍면동마다 다르므로 공통 대화 시나리오(운영시간 문의, 증명서 발급 문의 등 4~5개, 어느
동에나 통하는 일반적인 표현)만 넣고, 실제 위치·전화번호는 Kakao 검색 결과가 그대로 보여주게
한다(2번 지시의 17개 루프 keywordSearch 로직 그대로).

### 4) 교육어플 연결 메뉴

마커를 클릭했을 때 뜨는 기관 안내 화면(정보창)은 기존 계획대로 다국어 이용안내가 기본으로
나온다 — 여기는 바꾸지 않는다. 여기에 "직원 교육자료 보기" 같은 메뉴 버튼을 하나 추가한다.
누르면 `sports_app_bilingual.html`을 새 탭으로 연다(`target="_blank"`, 아이프레임으로 끼워
넣지 않는다 — 두 앱의 크기·상태가 서로 꼬일 수 있다).

이 버튼은 교육자료가 등록된 기관에만 보인다. 지금은 체육시설(복합스포츠센터) 하나뿐이니,
카테고리 데이터에 `trainingAppUrl` 같은 필드를 하나 추가해서 값이 있는 기관만 버튼을 보여주고
없는 기관은 버튼 자체를 숨긴다. 이렇게 해두면 앞으로 다른 기관 교육자료가 생길 때마다 이 필드
값만 채우면 되고 버튼 로직은 다시 손댈 필요가 없다.

배포 경로도 미리 정리한다. `아산시유관기관교육어플/sports_app_bilingual.html`은 지금 한글
폴더명 아래 있는데, 이 상태로 GitHub Pages에 올리면 링크 경로가 지저분해지거나 깨질 수 있다.
이 프로젝트(배포될 저장소) 안에 영문 경로로 복사해 넣는다 — 예를 들어 `edu/sports-center.html`
— 그리고 `trainingAppUrl`은 그 영문 경로를 가리키게 한다.

### 5) 반응형 웹앱 — 필수 요건 (2026-09-19 명시)

사용자가 명확히 요구했다: **컴퓨터와 스마트폰 모두에서 최적화돼 보여야 한다.** 아산시민뿐
아니라 다문화 가정·외국인 방문객이 실제로 쓸 앱이라, PC 화면이 아니라 스마트폰 화면에서
쓰는 경우가 오히려 더 많을 수 있다 — 반응형은 "나중에 확인할 사항"이 아니라 **이번 작업
전체가 지켜야 할 기본 전제**다.

- 기존 원본 `biz-eng-map`이 이미 반응형(데스크톱 900px 이상 / 모바일 390px)으로 만들어져
  있었다는 기록이 있다 — 이번 카테고리 재편(16개 칩, 체육시설·행정복지센터·교통시설처럼
  마커가 여러 개 겹치는 카테고리 추가)으로 이 반응형 레이아웃이 깨지지 않는지 **카테고리를
  다 바꾼 직후 매번** 확인한다.
- 특히 카테고리 칩이 16개로 늘어난 만큼, 모바일 폭(390px)에서 칩이 줄바꿈되거나 가로
  스크롤이 생기지 않는지, 지도·정보창·"직원 교육자료 보기" 버튼이 작은 화면에서도 손가락으로
  누르기 좋은 크기로 나오는지를 확인한다.
- 아래 "작업 후 확인"의 데스크톱/모바일 그리드 확인 항목은 이 요건의 검증 절차다 — 별개의
  선택 사항이 아니라 필수로 통과해야 하는 항목으로 취급한다.

## 건드리지 않는 것
- 번역 엔진(구글 비공식 엔드포인트 + MyMemory 폴백), 음성인식(Web Speech API), TTS, 12개
  언어 선택 UI — 전부 그대로 둔다.
- 도서관 좌석 조회 기능(`data/lib-index.json`, `lib-seats.json`, GitHub Actions 워크플로)도
  이번 작업에서는 건드리지 않는다. 유지할지 뺄지는 다음 단계에서 따로 정한다.

## 작업 후 확인
원본 `biz-eng-map`의 CLAUDE.md에 적힌 주의사항이 이 프로젝트에도 그대로 적용된다.
- 로컬 테스트는 `localhost:3000`으로만 한다(Kakao 지도 JS 키가 이 포트에 등록돼 있다).
- 카테고리 칩을 다 바꾼 뒤 데스크톱(900px 이상)과 모바일(390px) 양쪽에서 카테고리 그리드가
  제대로 보이는지 확인한다 — 원본에서 이 부분이 한 번 깨졌던 적이 있다.
- "직원 교육자료 보기" 버튼이 체육시설에서만 보이고 나머지 15개 카테고리에서는 보이지 않는지,
  눌렀을 때 `edu/sports-center.html`이 새 탭으로 정상적으로 열리는지 확인한다.
- 관광시설 카테고리에서 `AT4` 코드로 검색했을 때 현충사·외암민속마을·신정호지방정원 같은
  실제 관광지가 마커로 뜨는지 확인한다 — 안 뜨면 keywordSearch("아산시 관광지")로 바꾼다.
- **체육시설 카테고리**에서 5개 명칭(복합스포츠센터·이순신종합운동장·둔포국민체육센터·
  장애인국민체육센터·한마음야구장) 모두가 마커로 뜨는지 확인한다. 복합스포츠센터와
  이순신종합운동장은 주소가 같아 마커가 겹칠 수 있는데, 겹쳐도 문제없다(같은 부지이므로).
- **행정복지센터 카테고리**에서 17개 읍면동 이름으로 검색했을 때 몇 개나 실제로 잡히는지
  확인한다. "온양1동행정복지센터"는 이번에 확인했지만 나머지 16개(특히 읍·면 9곳)는
  이름 패턴이 다를 수 있다 — 안 잡히는 곳은 "{이름}주민자치센터", "{이름}사무소" 등으로
  재시도하고, 그래도 안 잡히면 그 동만 빠진 채로 두고 `기본정보_조사자료.md`에 어떤 동이
  빠졌는지 기록을 남긴다(지어내지 않는다).
- **교통시설 카테고리**에서 4개 명칭(천안아산역·아산역·아산고속버스터미널·아산시외버스터미널)
  keywordSearch로 실제 위치가 잡히는지 확인한다.
- 그룹 C(행정복지센터)는 일반적인 표현만 들어갔는지, 구체적인 정보를 지어내지 않았는지
  코드 리뷰로 확인한다.
