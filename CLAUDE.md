# CLAUDE.md — asan-sports-gongmojeon 작업 인계 문서

새 세션(대화)을 열 때 이 파일을 먼저 읽는다. 사용자(송정호 박사님)는 기억이 짧아,
이전 대화 내용을 다시 설명하지 않아도 여기서 상태를 파악해야 한다.

> ## ★ 새 세션이 가장 먼저 할 일 (2026-09-19 배포 완료)
>
> - **GitHub 저장소**: https://github.com/powercm77-git/asan-sports-gongmojeon (PUBLIC —
>   GitHub Pages로 서비스하는 예외 대상, biz-eng-map·healthyplan과 같은 정책).
> - **라이브 주소**: https://powercm77-git.github.io/asan-sports-gongmojeon/ — Pages
>   `legacy` 빌드(빌드 단계 없이 `main` 브랜치 루트를 그대로 서빙), 배포 직후 200 확인함.
> - **⚠️ 미완료 — 카카오 디벨로퍼스 도메인 등록**: 위 Pages 주소를 카카오 디벨로퍼스
>   콘솔 > 내 애플리케이션 > 플랫폼 > Web 허용 도메인에 아직 등록 안 했으면, 배포 주소에서
>   지도가 안 뜬다(`localhost:3000`만 등록돼 있을 가능성 큼). 다음 세션은 이게 됐는지부터
>   확인할 것 — 안 됐으면 사용자에게 직접 등록을 안내한다(이 작업은 코드 세션이 대신 할 수
>   없다, 카카오 계정 로그인 필요).
> - `.env`는 `.gitignore`로 제외돼 커밋 안 됐다. 카카오 JS 키(`index.html`에 하드코딩)는
>   클라이언트 노출이 정상인 키이고 도메인 제한으로 보호되니 공개 저장소에 있어도 문제없음.

## 1. 프로젝트가 뭔가

2026 아산시 AI·데이터 기반 행정 우수사례 공모전(공모기간 2026.9.1.~9.30., 참가대상 아산시청
전직원) 제출을 준비하는 프로젝트다. 실제 제출자는 아산시 복합스포츠센터에 근무하는 지인이고,
송정호 박사님은 소재 발굴과 기획, 프로그램 준비를 돕는 입장이다.

**사업명(확정)**: 다(多)함께 통(通)하는 아산 — 유관기관 다국어 이용안내 시스템
(복합스포츠센터 실증)

## 2. 이 프로젝트의 핵심 아이디어

`biz-eng-map`(업종별 영어 대화 지도, https://powercm77-git.github.io/biz-eng-map/)에 이미
구현되어 학교 현장에서 검증을 마친 다국어 회화 지원 엔진을, 아산시 유관기관 체계로 재구성해서
공공체육시설 창구 응대에 이식하는 것이 핵심이다. 새로 개발하는 게 아니라 **검증된 기술을 다른
현장에 옮겨 쓰는 것**이라는 점이 이 사례의 차별점이다 — 처음에는 "노쇼 예측 기반 동적 오버부킹"
같은 예측모델 방향도 검토했으나, 사용자가 "연구가 아닌 현장중심이어야 한다"고 방향을 바로잡아
지금의 형태로 정리됐다.

## 3. 이 폴더 안 구성

- `문서/다함께통하는아산_계획서.docx` — 공모전용 계획서(사업개요·추진배경·추진내용·활용기술·
  기대효과·확산계획·추진현황·참고자료 8절 구성). 아직 **아산시청의 실제 참가신청서 양식은
  확보하지 못했다** — 이 계획서는 내용 정리용이며, 실제 양식이 오면 그 틀에 맞춰 옮겨 담아야 한다.
- `index.html`, `package.json`, `package-lock.json`, `.gitignore`, `.env.example`,
  `data/lib-index.json`, `data/lib-seats.json` — `biz-eng-map`에서 가져온 프로그래밍 시작점이다.
  **이미 한 차례 코드 작업이 진행됐다** — 자세한 내용은 "4. 프로그래밍 작업 — 진행 상황" 참고.
- `아산시유관기관교육어플/sports_app_bilingual.html` — 송정호 박사님이 복합스포츠센터
  직원용으로 이미 만들어 둔 별도의 독립 자체학습 앱이다(`index.html`과는 무관한 별개 파일).
  운영시간·시설 위치·대관 신청·빙상장·탁구장·스쿼시장·배드민턴장·농구장/배구장·탈의실·
  샤워장·화장실·분실물·주차장·장비대여·요금·응급상황·대중교통, 총 17개 실제 현장 상황을
  영어·일본어 쉬운 표현으로 정리했고, 단어·발음·역할극 대화·확인 퀴즈 네 단계로 구성돼
  있다. 여기 담긴 한국어-영어 대화 세트(`LANG_DATA.en.dial`)는 `index.html`의 체육시설
  카테고리 콘텐츠를 채울 때 지어내지 않고 그대로 옮겨 쓸 원천 자료다 — 자세한 지시는
  `작업지시서_01_카테고리재편.md`의 "3) 카테고리별 대화 콘텐츠" 참고.
- `기본정보_조사자료.md` — 나머지 15개 카테고리(체육시설 제외)의 실제 운영시간·위치·입장료·
  업무 내용을 조사해 정리한 자료. [확인됨]/[추가확인필요]로 표시돼 있다 — 코드 세션은 여기
  없는 내용을 지어내면 안 된다.

## 4. 프로그래밍 작업 — 진행 상황 (2026-09-19 개정 작업지시서 실행 완료, 시각 확인만 남음)

`작업지시서_01_카테고리재편.md`(2026-09-19 재개정판)가 실행됐다. 16개 카테고리 전부 실제
콘텐츠를 채우는 것까지 이번에 끝났다 — 아래는 그 결과다.

**완료됨:**
1. **16개 카테고리 확정** — 체육시설(SPT)·보건소(HLC)·행정복지센터(GOV)·아산가족센터(FAM)·
   문화시설(CT1)·도서관(LIB)·과학관·곤충원(SCI)·청소년문화센터(YTH)·여성새일센터(JOB)·
   육아종합지원센터(CHD)·농업기술센터(AGR)·평생학습관(EDU)·**관광시설(TOUR, 신규)**·
   **병원(HP8, 신규)**·**약국(PM9, 신규)**·**교통시설(TRN, 신규)**.
2. **체육시설·행정복지센터·교통시설을 "멀티 키워드 검색"으로 재구현** — `KEYWORD_QUERY`의
   `SPT`(10개 시설명)·`GOV`(17개 읍면동 이름)·`TRN`(4개 역·터미널명)을 문자열 배열로 바꾸고,
   새로 만든 `multiKeywordSearch()` 함수가 배열의 각 이름을 개별 `keywordSearch`로 돌려
   1위 결과만 모아(id 중복 제거) 한 카테고리 레이어로 합친다. **위치 기준을 현재 지도
   중심이 아니라 항상 아산시청 좌표(`DEFAULT_CENTER`) + 반경 20km(Kakao 허용 최댓값)로
   고정했다** — 기관들이 시 전역에 흩어져 있어서, 지도를 패닝할 때마다 위치가 바뀌는 기존
   방식(`radius:1200`, 현재 지도 중심 기준)으로는 애초에 대부분 안 잡혔을 것이기 때문이다.
   이건 지시서에 명시되지 않은 판단이지만, 멀티 키워드 기능이 실제로 동작하려면 필요한
   전제였다. **아직 실제 Kakao API로 몇 개나 잡히는지는 못 봤다** — 5번 "시각 확인" 참고.
   나머지 단일 기관(HLC·FAM·SCI·YTH·JOB·CHD·AGR·EDU)은 기존처럼 현재 지도 중심 기준
   `radius:1200` 단일 키워드 검색 그대로 뒀다 — 이 방식도 지도가 기관에서 1.2km 밖으로
   벗어나면 "못 찾음"이 뜰 수 있는 잠재적 문제이지만, 이번 지시 범위 밖이라 손대지 않았다.
3. **관광시설(TOUR)·병원(HP8)·약국(PM9)은 Kakao 표준 코드로 `categorySearch`** — `AT4`·
   `HP8`·`PM9`는 `KEYWORD_QUERY`에 없어서 기존 `else` 분기(categorySearch)로 자동 처리된다.
   문화시설(CT1)과 같은 방식.
4. **체육시설(SPT) 시나리오 17개 실제 자료로 전면 교체** — 이전에 지어냈던 수영장·헬스장
   내용을 전부 지우고, `아산시유관기관교육어플/sports_app_bilingual.html`의
   `LANG_DATA.en.dial`(17개 상황, 상황당 10개 대화 변형 중 첫 번째 변형)을 그대로 옮겼다.
   발음 표기(`.pron`)만 이 세션에서 새로 붙였다(원본엔 없었음). 같이 뜨는 나머지 9개
   체육시설(이순신종합운동장 등)은 전용 시나리오 없이 Kakao 기본 정보만 노출 — 작업지시서
   그룹 A 방침대로다.
5. **병원(HP8)·약국(PM9)·교통시설(TRN) 시나리오는 원본 `biz-eng-map`에서 그대로/일부 수정
   재사용** — HP8·PM9는 완전 그대로(각 15개). 교통시설은 원본 지하철(SW8) 시나리오 15개 중
   "몇 호선"·"3호선 환승"처럼 지하철에만 해당하는 표현 4곳을 KTX역·버스터미널에도 통하는
   중립적 표현으로 고쳐서(예: "승강장 몇 번" 방식으로) `TRN` 키로 재사용했다.
6. **관광시설(TOUR)·보건소(HLC)·아산가족센터(FAM)·과학관곤충원(SCI)·청소년문화센터(YTH)·
   여성새일센터(JOB)·육아종합지원센터(CHD)·농업기술센터(AGR)·평생학습관(EDU) — 그룹 B
   9개 카테고리 실제 콘텐츠 작성** — `기본정보_조사자료.md`의 [확인됨] 주소·전화번호·
   운영시간을 반영했다. 확정 사실이 주소/전화 정도뿐인 기관(HLC 등)은 그 기관에서 보편적으로
   일어나는 업무(예방접종·상담 예약 등)를 소재로 삼되, 확인 안 된 수치(요금·프로그램명 등)는
   지어내지 않았다 — HP8/PM9를 "보편적 방문 대화라 재사용해도 지어낸 게 아니다"로 판단했던
   것과 같은 기준을 적용했다.
7. **행정복지센터(GOV)는 그룹 C 그대로 유지** — 4개 범용 표현(운영시간/위치·주차/서류/담당자
   연결) 그대로, 지어낸 구체 정보 없음. 검색만 17개 읍면동 루프로 바뀜(2번 참고).
8. **"직원 교육자료 보기" 버튼 추가** — `TRAINING_APP_URL` 맵(현재 `SPT`만 값 있음)에 따라
   정보 시트에 버튼이 뜨고 안 뜨고가 결정된다. 누르면 새 탭(`target="_blank"`)으로
   `edu/sports-center.html`이 열린다 — `아산시유관기관교육어플/sports_app_bilingual.html`을
   한글 경로 문제 없는 영문 경로로 그대로 복사해뒀다(수정 없음, 원본 그대로).
9. **다국어 번역·음성인식·TTS 엔진, 도서관 좌석 기능** — 전부 그대로 재사용 중, 이번에도
   손대지 않았다.
10. **Node로 JS 구문 검사 + 데이터 정합성 검사 완료** — `node --check`로 문법 확인, SCENES·
    `CATEGORY_LABEL`·`CATEGORY_ICON`·`STAFF_LABEL` 16개 키 전부 일치, HTML의 `data-cat`
    속성도 16개 코드가 칩·그리드 양쪽에 정확히 2번씩(총 32개) 있는 것까지 스크립트로 확인.
    CSS도 읽어봤다 — `.cat-row`는 원래 `overflow-x:auto`(가로 스크롤)로 설계돼 있고
    `.cat-grid`는 `grid-template-columns: repeat(4, 1fr)`라서 칩이 12개→16개로 늘어도
    구조적으로 깨질 이유는 없어 보인다(다만 이건 코드를 읽고 추론한 것이지 눈으로 본 게
    아니다 — 5번 참고).

**시각 확인 — 완료됨.** 16개 카테고리·"직원 교육자료 보기" 버튼·반응형(900px+/390px)까지
브라우저로 직접 확인 완료, 그 과정에서 버그 2건(관광시설 `TOUR`→`AT4` 키 불일치, 단일
키워드 카테고리 8개의 "지도 드리프트")을 발견·수정했다. **자세한 경위·재현 방법·수정
내용은 9번·10번 항목 참고 — 이 두 수정은 되돌리지 말 것.**

## 5. 배포 상태 (2026-09-19)

- **GitHub 저장소**: https://github.com/powercm77-git/asan-sports-gongmojeon (PUBLIC)
- **라이브 주소**: https://powercm77-git.github.io/asan-sports-gongmojeon/ — Pages
  `legacy` 빌드, 최신 커밋(`480d4cd`)까지 `built` 상태 확인, 200 응답 확인.
- **⚠️ 미완료**: 카카오 디벨로퍼스 콘솔에 위 Pages 도메인을 Web 허용 도메인으로 등록하는
  작업. 코드 세션이 대신 할 수 없다(카카오 계정 로그인 필요) — 사용자가 직접 해야 한다.
  안 하면 배포 주소에서 지도가 안 뜬다.

## 6. 다음에 확인할 것 (우선순위 순)

1. **카카오 디벨로퍼스에 Pages 도메인 등록됐는지 확인** — 안 됐으면 사용자에게 안내.
2. 그 지인(복합스포츠센터 근무)에게 실제 다문화 이용객 언어권·빈도를 확인했는지
3. 아산시청 공모전 참가신청서 정식 양식을 받았는지

## 7. 이번에 추가로 정리된 것 (2026-09-18)

- 사용자가 관광 확장 아이디어를 제안했다 — 아산시가 "방문의 해"·웰니스관광으로 관광 활성화에
  힘쓰고 있고, 관광객 중에는 다문화가족·외국인도 있으며 최근 체육·문화시설 이용도 느는
  추세이니, 지금 아산시민 대상 시스템을 잘 기획하면 타지역·외국인 방문객도 쓸 수 있는
  기획이 될 수 있다는 내용이다. 실제 정책 근거(이데일리 보도 등)로 뒷받침되는 타당한
  방향이지만, 공모전 핵심 스토리를 흐리지 않도록 계획서 "확산 계획"에 짧은 한 줄로만
  반영했다 — 지금 단계에서 시스템을 관광객용으로 새로 설계하지 않는다.
- 사용자가 복합스포츠센터 직원용으로 이미 만들어 둔 자체학습 앱(`아산시유관기관교육어플/
  sports_app_bilingual.html`)을 공유했다. 17개 실제 상황·영어/일본어 쉬운 표현·단어/발음/
  대화/퀴즈 구성으로, "이미 검증된 기존 성과"를 뒷받침하는 근거로 계획서 추진내용·추진현황에
  반영했고, 확산 계획에는 기관별 확산 시 직원 교육자료도 함께 준비한다는 내용을 추가했다.
  이 앱의 한국어-영어 대화 세트는 `index.html` 체육시설 카테고리 콘텐츠의 원천 자료로도
  쓰기로 하고 `작업지시서_01_카테고리재편.md`를 그에 맞게 수정했다.
- 사용자가 두 앱의 연결 방식을 제안했다: `index.html`에서 기관(예: 복합스포츠센터)을 클릭하면
  기존 계획대로 다국어 이용안내가 기본으로 뜨고, 여기에 "직원 교육자료 보기" 메뉴 버튼을 추가해
  누르면 `sports_app_bilingual.html`이 새 탭으로 열리는 구조다. 콘텐츠를 옮겨 담는 것과
  별개로, 이미 완성된 두 앱을 그대로 둔 채 링크만 연결하는 방식이라 작업량이 적고 두 앱 다
  건드릴 일이 없다. 교육자료가 없는 기관은 버튼이 안 보이게 하고, 앞으로 다른 기관 교육자료가
  생기면 데이터 필드(`trainingAppUrl`)만 채우면 되게 설계하기로 했다. 배포 시 한글 폴더명
  문제를 피하려고 `sports_app_bilingual.html`은 영문 경로(`edu/sports-center.html`)로 복사해
  프로젝트 안에 넣기로 했다. 자세한 지시는 `작업지시서_01_카테고리재편.md`의 "4) 교육어플
  연결 메뉴" 참고.

## 8. 카카오맵 위치 재검증 · 카테고리 재설계 (2026-09-19)

사용자가 두 가지를 요구했다 — (1) 위치 정보는 일반 웹검색 말고 카카오맵으로 직접 확인할 것,
(2) 체육시설·문화시설·행정기관 등 카테고리 구성을 "내가 생각하는 최적"으로 다시 설계할 것.
이 세션에서 빌트인 브라우저(Claude 데스크톱 앱 안의 브라우저 창 — 사용자의 실제 Chrome
확장은 이 세션에서 연결되지 않아 대신 사용)로 map.kakao.com에 직접 접속해 기관명을 하나씩
검색했다.

**발견한 오류 2건** — `기본정보_조사자료.md` "이번에 정정된 것" 참고:
- 보건소 주소가 사실은 아산시청 주소였다(둘이 뒤섞여 있었음).
- 아산가족센터 주소가 실제와 달랐다(온양원도심문화복합시설이 아니라 아산로 13).
- (외암민속마을 주소도 저잣거리 주소와 본체 주소가 섞여 있어 정정)

**[추가확인필요] 4건 중 3건 해소**: 여성새로일하기센터·통합일자리지원센터(같은 건물 확인),
아산시육아종합지원센터(위치 확인), 평생학습문화센터(본관 위치 확인). 행정복지센터만 여전히
[추가확인필요]로 남아 있는데, 이건 애초에 "17개 읍면동마다 다르다"는 성격 때문이지 정보를
못 찾은 게 아니다 — 오히려 이번에 17개 행정구역 전체 목록(염치읍·배방읍 2개 읍, 송악면 등
9개 면, 온양1~6동 6개 행정동)을 확인했고, "온양1동행정복지센터"로 검색하면 정확히 잡힌다는
것도 확인했다. 그래서 "대표 정보만" 방침을 버리고 **17개 이름을 전부 반복 검색하는 루프**로
설계를 바꿨다.

**카테고리 구성 재설계**: 사용자가 지적한 진짜 문제는, 체육시설 카테고리가 복합스포츠센터
하나만 검색해서 아산시 전체를 대표하지 못한다는 점이었다. 그래서 체육시설·행정복지센터·
교통시설 3개 카테고리를 "기관명 하나 검색"에서 "여러 기관명을 함께 검색해 합치기"(멀티
키워드) 방식으로 바꿨다:
- 체육시설: 복합스포츠센터 외에 이순신종합운동장(카카오 확인 결과 복합스포츠센터와 같은
  부지·같은 전화번호였다)·둔포국민체육센터·아산장애인국민체육센터·한마음야구장 4곳을 추가.
- 행정복지센터: 17개 읍면동 이름 전체를 반복 검색.
- 교통시설: 원래 계획이던 `SW8`(지하철역) 코드는 KTX역·버스터미널을 못 잡을 가능성이 높아
  건너뛰고, 천안아산역·아산역·아산고속버스터미널·아산시외버스터미널 4곳을 바로 확정.

카테고리 개수(16개)는 바뀌지 않았다 — 바뀐 건 저 3개 카테고리 **안에 마커가 몇 개 뜨는가**다.
자세한 지시는 개정된 `작업지시서_01_카테고리재편.md`의 2)·3)번, 조사 근거는
`기본정보_조사자료.md`를 참고. 아직 코드에는 반영 안 됐다 — 다음 코드 세션에 이 개정된
지시서를 넘겨야 한다.

## 9. 실제 브라우저로 시각 확인 + 버그 1건 발견·수정 (2026-09-19, 4번 항목 실행 이후)

4번 항목(16개 카테고리 코드 반영, TOUR/HP8/PM9/TRN 추가, 멀티 키워드 구현)이 실행된 뒤,
이 세션에서 빌트인 브라우저로 `localhost:3000`에 직접 접속해 "시각 확인" 목록을 실제로
확인했다. 아래는 그 결과다 — **4번에 적힌 "아직 못 봄" 상태는 이제 아래로 갱신됐다.**

**정상 확인됨:**
- 체육시설: 10곳 전부 마커·목록으로 정상 표시.
- 행정복지센터: 17개 읍면동 **전부** 잡혔다(우려했던 "읍·면은 이름 패턴이 다를 수 있다"는
  기우였음 — `"{이름}행정복지센터"` 패턴 하나로 17개 다 통했다).
- 병원(HP8): 15곳 정상 표시.

**경미한 것(고칠 필요 없어 보임):**
- 교통시설: 4곳 등록했는데 3곳만 뜬다. 원인: `multiKeywordSearch`가 결과를 카카오 place id로
  중복 제거하는데, "아산역"으로 검색해도 카카오가 1위 결과로 "천안아산역"을 주는 것으로
  보여 두 검색어가 같은 id로 합쳐진다. 실제로 두 역은 같은 건물(배방읍 희망로 90/100)이라
  사용자가 어느 이름으로 찾아도 같은 곳에 도착하므로, 굳이 손대지 않아도 된다고 판단했다.

**버그 발견 → 이 세션에서 직접 수정·커밋함:**
- **관광시설이 완전히 비어 있었다("주변에서 찾지 못했습니다")**. 원인 2가지를 코드에서
  확인:
  1. 내부 카테고리 키를 `TOUR`로 만들어 놓고 `categorySearch(catCode, ...)`에 그 값을 그대로
     넘기고 있었다 — Kakao의 실제 관광명소 코드는 `AT4`라 API가 못 알아들었다.
  2. 설사 코드가 맞았어도, 이 카테고리만 다른 일반 카테고리처럼 "현재 지도 중심 + 1.2km"
     방식이라 현충사·외암민속마을처럼 아산시청에서 여러 km 떨어진 곳은 애초에 반경 밖이었다.
  - **수정**: 내부 키를 `TOUR`→`AT4`로 전체 통일(칩 버튼 2곳, `CATEGORY_LABEL` 등 5개 맵,
    `SCENES` 키, 총 8곳)하고, `AT4`만 체육시설·행정복지센터처럼 아산시청 좌표+20km 고정
    검색으로 바꿨다. `node --check` 통과 확인 후 `index.html`에 커밋 완료.
  - 카카오 API를 페이지 콘솔에서 직접 호출해 `AT4` 코드 자체는 정상 동작(15곳, "온양온천"
    등)하는 것도 확인했다 — 코드 로직 문제였지 API·데이터 문제가 아니었다.

**파일 되돌아감 사고 → 복구:** 위 수정을 커밋한 직후, `index.html`이 원본
`biz-eng-map`(179,152바이트, "취업역량강화" 학교 브랜딩)으로 되돌아가 있는 게 발견됐다.
정확한 원인은 끝내 특정하지 못했다(이 데스크톱엔 터미널 접근이 없어 프로세스·git 이력을
볼 수 없었음). 코드 세션이 살아있고 유휴 상태임을 사용자에게 확인받은 뒤, 수정된 사본을
mtime 확인하며 다시 커밋해 안정된 상태로 되돌렸다.

## 10. 나머지 15개 카테고리 + 버튼 + 반응형까지 전체 재확인 완료 (2026-09-19, 9번 이어서)

파일 복구 후 관광시설(AT4)부터 재확인을 이어갔고, 이번엔 16개 카테고리 전부·직원 교육자료
버튼·반응형(모바일 390px)까지 빠짐없이 브라우저로 직접 확인했다. **이제 "아직 못 봄" 항목은
없다.**

**관광시설(AT4) 재확인 — 정상:**
- 9번에서 고친 코드가 그대로 살아있음을 재확인. 15곳 정상 표시(온양온천 등 포함).

**추가로 발견·수정한 버그 — 단일 키워드 카테고리 8개의 "지도 드리프트" 문제:**
- 대상: 보건소(HLC)·아산가족센터(FAM)·과학관·곤충원(SCI)·청소년문화센터(YTH)·
  여성새일센터(JOB)·육아종합지원센터(CHD)·농업기술센터(AGR)·평생학습관(EDU) — 전부
  "아산시 안 특정 기관 하나"를 찾는 카테고리.
- 증상: 지도를 패닝한 **현재 지도 중심**에서 1.2km 반경으로 검색하고 있어서, 사용자가
  관광시설 등을 보고 지도가 조금만 움직인 상태에서 이 카테고리를 누르면 엉뚱한 동명 기관이
  잡힐 수 있었다. 실제로 재현: 지도 중심이 966m 벗어난 상태에서 "아산가족센터"를 검색하니
  아산로 13의 진짜 아산시가족센터 대신 "아산시다문화가족지원센터 1센터"(시장길 29)가
  잡혔다.
- 원인은 브라우저 콘솔에서 지도 중심 기준 검색과 `DEFAULT_CENTER` 기준 검색 결과를 직접
  비교해 확정했고, 카카오 API 자체는 정상이었음도 함께 확인했다.
- **수정**: 관광시설(AT4)에 적용한 것과 같은 방식으로, 이 8개 카테고리도 지도 중심이 아니라
  아산시청 좌표(`DEFAULT_CENTER`) + 20km 고정 검색으로 바꿨다. `node --check` 통과 확인 후
  `index.html`에 커밋 완료.
- 재확인: 아산가족센터(아산시가족센터가 1위로 정상), 농업기술센터·과학관곤충원·
  청소년문화센터·여성새일센터·육아종합지원센터·평생학습관도 모두 정상 또는 허용 범위 내
  결과로 확인.

**16개 카테고리 전체 시각 확인 — 완료 (체육시설·보건소·행정복지센터·아산가족센터·문화시설·
도서관·과학관곤충원·청소년문화센터·여성새일센터·육아종합지원센터·농업기술센터·평생학습관·
관광시설·병원·약국·교통시설, 총 16개 전부).**

**"직원 교육자료 보기" 버튼 — 정상:**
- 체육시설(SPT) 중 아산시복합스포츠센터를 클릭하면 버튼이 뜨고, 클릭하면
  `edu/sports-center.html`("복합스포츠센터 영어 안내 학습")이 새 탭에서 정상적으로 열린다.
- 보건소(HLC) 등 `TRAINING_APP_URL`에 값이 없는 카테고리에서는 버튼이 뜨지 않음을 확인 —
  의도대로 체육시설에만 노출된다.

**반응형(모바일 390px) — 정상:**
- 뷰포트를 390×844(iPhone 크기)로 강제한 뒤 확인. 헤더·카테고리 칩·검색/내위치/지도/위성/
  번역 툴바·상태 문구·지도가 모두 겹침·잘림 없이 화면에 맞게 배치됨.
- 카테고리 칩 목록(16개)은 가로 스크롤 컨테이너(`.cat-row`, `overflow-x: auto`)로
  구현되어 있어 화면엔 일부만 보이지만 스와이프로 전부 접근 가능함을 DOM/스크롤 테스트로
  확인(`scrollWidth` 1898px vs `clientWidth` 310px).
- 마커를 눌렀을 때 뜨는 정보 시트는 하단에서 올라오는 바텀시트 형태로, 제목·주소·정보/로드뷰/
  대화 탭·길찾기 버튼·직원 교육자료 보기 버튼·전화번호까지 390px 폭에서 잘림 없이 전부 표시됨.
- 데스크톱(900px+)은 4번·9번 항목에서 이미 확인됐고 이번에도 이상 없음.

**경미한 것(고칠 필요 없다고 판단, 참고용 기록):**
- 교통시설 4곳 중 1곳(아산역/천안아산역)이 같은 place id로 중복 제거되어 3개로 표시됨 —
  두 역이 물리적으로 같은 건물이라 실질적 정보 손실은 아님.
- 평생학습관: 내용에 맞는 "아산시평생학습문화센터"가 카카오 검색 결과 15곳 중 4위로 뜸(1~3위는
  다른 동별 평생학습센터 지점) — 검색 결과 안에는 포함되어 있어 문제로 보지 않음.
- 농업기술센터·과학관곤충원: 반경을 20km로 넓히면서 천안시·예산군 소재 기관이나 대학
  과학관 등 "노이즈"가 일부 섞여 나옴 — 정답 기관 자체는 정상 1위로 나오므로 우선순위
  낮은 개선 사항으로만 기록.

**결론**: 16개 카테고리·직원 교육자료 버튼·반응형(데스크톱+모바일) 모두 실제 브라우저로
확인 완료. 남은 건 위 "경미한 것" 3건뿐이며, 사용자 요청이 없는 한 추가로 손대지 않는다.

## 11. UI 문체 원칙 — 시스템 안내 문구는 하십시오체로 통일 (2026-09-19, 중요·반복 위반 주의)

사용자가 이 문제로 여러 차례 지적했다 — GitHub Pages 배포 후 화면 문구가 "번역체"·
"유치원생 안내장" 같다는 지적을 두 번 받았다. 원인은 이 프로젝트가 `biz-eng-map`(상점
영어회화 앱)에서 코드를 그대로 가져오면서 문구도 같이 따라와, 공공기관 안내 앱에 안 맞는
캐주얼한 말투("가게", "~하세요", "~해보세요")가 여기저기 남아 있었기 때문이다. 앞으로
**이 원칙을 지키지 않으면 같은 지적이 또 나온다.**

**원칙**: `index.html`에서 사용자에게 보이는 문구는 두 종류로 구분해서 다르게 쓴다.

1. **시스템 안내 문구(헤더·부제·상태 메시지·에러 메시지·플레이스홀더·버튼 안내)** —
   공공기관 안내문 문체로 통일한다.
   - 종결어미는 하십시오체(`~합니다`/`~됩니다`/`~해 주십시오`)만 쓴다. `~해요`/`~하세요`/
     `~해보세요`/`~해주세요` 같은 해요체는 절대 섞지 않는다(반말+요는 존댓말이어도 공문서
     체가 아니다).
   - 문장은 짧고 간결하게, 한 문장에 한 가지만 담는다. "그 기관에서" "바로 쓸 수 있는"처럼
     같은 말을 반복하거나 설명을 덧붙이는 버릇을 피한다 — 길어질수록 오히려 안내장처럼
     읽힌다. 예: "지도에서 시설을 선택하면 다국어 대화 예시가 제공됩니다."(o) /
     "지도에서 기관을 선택하면 해당 기관에서 활용할 수 있는 다국어 대화가 나타납니다"(x,
     늘어짐).
   - 지시어는 "해당"을 쓴다("그 기관" 대신 "해당 기관").
   - **"기관"과 "시설"을 구분해서 쓴다** — 16개 카테고리 중 보건소·행정복지센터·
     아산가족센터·여성새일센터처럼 행정 서비스를 제공하는 곳은 "기관"이 맞지만,
     체육시설·도서관·관광시설·병원·약국처럼 물리적 장소는 한국어로 "기관"이라고 잘 안
     부른다(2026-09-19 사용자 지적). 16개를 통째로 가리킬 때는 절반만 맞는 "기관" 대신,
     양쪽을 다 자연스럽게 포함하는 "시설"을 쓴다. 단, 헤더의 "유관기관 다국어 이용안내"는
     이미 확정된 사업명 표기라 그대로 둔다 — 이건 사업명이지 안내 문장이 아니다.
2. **SCENES 대화 콘텐츠(직원-시민 예시 대화, `ko:` 필드)** — 이건 반대로 **손대지 않는다.**
   실제 사람이 창구에서 주고받는 구어체 대화를 보여주는 학습 콘텐츠라서 "~하세요?"
   "~되나요?" 같은 해요체가 오히려 맞다. 여기를 하십시오체로 바꾸면 안 된다.

**이번에 시스템 문구 전수 검사해서 고친 것**(SCENES 대화 내용은 대상에서 제외하고
`textContent`/`placeholder`/상수로 정의된 안내·에러 메시지만 검사):
- subtitle, `<title>`, 번역 입력창 placeholder, 번역 참고 문구(`.translate-note`)
- 지도 관련 안내: "찾지 못했습니다" 뒤 안내, 지역 검색 결과 없음 안내
- 번역 실패 메시지 2종(`TRANSLATE_KO_FAILED_MSG`/`TRANSLATE_EN_FAILED_MSG`), 번역 API
  실패 메시지
- 음성 인식 관련 안내 3종(듣는 중/인식 실패/시작 불가)

다음 세션이 이 파일에 새 안내 문구를 추가할 때는 반드시 이 원칙을 먼저 확인하고 쓸 것.
